import { TransactionT } from '../../../core/transaction';
export declare class FakeTransaction implements TransactionT {
    private parentTransaction;
    constructor(name: string, parentTransaction?: TransactionT | undefined);
    private _name;
    readonly name: string;
    readonly fullName: string;
    private _complete;
    readonly isComplete: boolean;
    rollback(): Promise<void>;
    commit(): Promise<void>;
}
