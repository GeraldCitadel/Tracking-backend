import { ServerMetadataT } from '../metadata/server/server-t';
import { Task, TaskMetadataT } from '../metadata/server/task-t';
import { Server } from './server';
export declare class Miter {
    static launch(meta: ServerMetadataT): Promise<Server>;
    static task(meta: Task | TaskMetadataT): (...args: string[]) => Promise<void>;
    static normalizePort(val: any): string | number;
}
