import { Server } from '../server/server';
import { ServerMetadata } from '../metadata/server/server';
import { OrmMetadata } from '../metadata/server/orm';
import { Logger } from './logger';
export declare class ReplService {
    private server;
    private logger;
    private meta;
    private ormMeta;
    constructor(server: Server, logger: Logger, meta: ServerMetadata, ormMeta: OrmMetadata);
    private context;
    private loopPromise;
    start(): Promise<void>;
    private proxyModel(model);
    private makeContext();
    private resolvePromise(promise);
    private execute(code);
    private repl();
    stop(): Promise<void>;
}
