import { TransactionT } from '../../core/transaction';
export declare class FakeTransactionService {
    constructor();
    start(): Promise<void>;
    readonly current: TransactionT | undefined;
    run<T = void>(transactionName: string, fn: () => Promise<T>): Promise<T>;
}
