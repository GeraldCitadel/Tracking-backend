import { CtorT } from '../../core/ctor';
import { InjectableMetadata } from '../../metadata/services/injectable';
export declare function Injectable<T>(meta?: InjectableMetadata<T>): (injectable: CtorT<T>) => void;
