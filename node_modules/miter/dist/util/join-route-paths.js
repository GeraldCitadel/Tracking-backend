"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function joinRoutePaths(...paths) {
    let partial = '/';
    for (let q = 0; q < paths.length; q++) {
        let path = paths[q];
        if (!path || path == '/')
            continue;
        if (path.startsWith('/'))
            path = path.substring(1);
        if (path && !partial.endsWith('/'))
            partial += '/';
        partial += path;
    }
    return partial;
}
exports.joinRoutePaths = joinRoutePaths;
//# sourceMappingURL=join-route-paths.js.map