/// <reference types="node" />
import { JwtMetadataT } from './jwt-t';
export declare class JwtMetadata {
    private _meta;
    constructor(_meta: JwtMetadataT);
    readonly originalMeta: JwtMetadataT;
    readonly secret: string | Buffer;
    readonly tokenProperty: string;
}
