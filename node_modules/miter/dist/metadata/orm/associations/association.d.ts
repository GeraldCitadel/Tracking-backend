import { StaticModelT, ModelT } from '../../../core/model';
export declare type ForeignModelSource = StaticModelT<ModelT<any>> | {
    (): StaticModelT<ModelT<any>>;
} | {
    modelName: string;
} | {
    tableName: string;
};
export interface AssociationMetadata {
    foreignModel?: ForeignModelSource;
    constraints?: boolean;
}
